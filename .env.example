# Django settings
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=true
ALLOWED_HOSTS=127.0.0.1,localhost

# PostgreSQL connection (matches docker-compose.yml defaults)
DB_NAME=sda_book_index
DB_USER=sda
DB_PASSWORD=sda
DB_HOST=127.0.0.1
DB_PORT=5432

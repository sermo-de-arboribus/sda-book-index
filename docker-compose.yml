services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: sda_book_index
      POSTGRES_USER: sda
      POSTGRES_PASSWORD: sda
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
